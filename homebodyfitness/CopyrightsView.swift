
import SwiftUI

struct CopyrightsView: View {
    var body: some View {
			VStack(alignment: .leading, spacing: 5) {				
				Text("Fonts used:")
				Text("https://fonts.google.com/specimen/Oxanium")
				Text("Workouts GIFs used from:")
				Text("https://tenor.com/search/workout-gifs")
				Text("Icons:")
				Text("https://www.flaticon.com/packs/yoga-pose-1")
			}
    }
}
